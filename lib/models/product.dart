import 'package:flutter/cupertino.dart';

class Product {
  String name;
  double price;
  String productImageUrl;
  int quantity;

  Product(this.name, this.price, this.productImageUrl, this.quantity);
}
